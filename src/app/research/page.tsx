import { ChartLineUp, Database, SealCheck } from "@phosphor-icons/react/dist/ssr";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";

export default function ResearchPage() {
  return <main><SiteHeader active="Research" /><section className="page"><div className="shell"><span className="eyebrow">Research record / guardrails</span><h1>Results only mean something if the test survives the model.</h1><p>The humanized-AI work uses prompt-lineage-safe partitions, separate development and calibration stages, and conservative review bands. This makes limitations legible rather than hidden.</p><div className="content-grid"><article className="content-card"><Database size={28} color="var(--signal)" /><h2>Lineage-aware data</h2><p>Related samples remain together across splits so a model cannot earn a result by memorising prompt-family traces.</p></article><article className="content-card"><ChartLineUp size={28} color="var(--signal)" /><h2>Conservative review</h2><p>The transformed-AI lane only raises a review signal when its evidence is unusually strong, limiting avoidable false alarms.</p></article><article className="content-card"><SealCheck size={28} color="var(--signal)" /><h2>Known limitation</h2><p>Carefully expert-edited AI was close to chance in the selected evaluation. The UI explicitly discloses that weakness.</p></article></div></div></section><SiteFooter /></main>;
}
