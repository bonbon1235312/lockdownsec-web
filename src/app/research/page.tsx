import { ChartLineUp, Database, SealCheck } from "@phosphor-icons/react/dist/ssr";
import { SiteHeader } from "@/components/site-header";

export default function ResearchPage() {
  return <main><SiteHeader active="Research" /><section className="page"><div className="shell"><span className="eyebrow">Research record / guardrails</span><h1>Results only mean something if the test survives the model.</h1><p>The humanized-AI work uses prompt-lineage-safe partitions, separate development and calibration stages, and fixed operating thresholds. This makes the limitations legible rather than hidden.</p><div className="content-grid"><article className="content-card"><Database size={28} color="var(--signal)" /><h2>Lineage-aware data</h2><p>Related samples remain together across splits so a model cannot earn a score by memorising prompt-family traces.</p></article><article className="content-card"><ChartLineUp size={28} color="var(--signal)" /><h2>Low-FPR focus</h2><p>The humanizer is deployed at a 5% calibration-split human false-positive operating point, with stricter thresholds retained for inspection.</p></article><article className="content-card"><SealCheck size={28} color="var(--signal)" /><h2>Known limitation</h2><p>Carefully expert-edited AI was close to chance in the selected evaluation. The UI explicitly discloses that weakness.</p></article></div></div></section></main>;
}
