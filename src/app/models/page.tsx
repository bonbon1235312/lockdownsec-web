import { Cpu, Gauge, Stack } from "@phosphor-icons/react/dist/ssr";
import { SiteHeader } from "@/components/site-header";

export default function ModelsPage() {
  return <main><SiteHeader active="Models" /><section className="page"><div className="shell"><span className="eyebrow">Deployed models / artifact facts</span><h1>Small, separate, inspectable.</h1><p>The site deploys two selected artifacts. It does not claim a single universal AI-authorship probability or conceal an ensemble behind a simplified label.</p><div className="content-grid"><article className="content-card"><Cpu size={28} color="var(--signal)" /><h2>Raw AI detector</h2><p>ONNX deployment artifact using byte-BPE inputs. Its displayed score is experimental and unscaled; it is not presented as a calibrated probability.</p></article><article className="content-card"><Stack size={28} color="var(--signal)" /><h2>Humanizer V4.8</h2><p>PyTorch multi-window transformer with structural features. It uses frozen score thresholds selected from a separate calibration split.</p></article><article className="content-card"><Gauge size={28} color="var(--signal)" /><h2>Operating bands</h2><p>Low below 0.931342. Elevated from 0.931342 (5% human-FPR point). High from 0.947199 (2% human-FPR point).</p></article></div><div className="page-note">V5.1 was a 13,228,289-parameter archival experiment. It did not meet the pre-set promotion rule, so it is not deployed.</div></div></section></main>;
}
