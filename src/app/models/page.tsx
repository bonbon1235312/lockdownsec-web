import { Cpu, Gauge, Stack } from "@phosphor-icons/react/dist/ssr";
import { SiteHeader } from "@/components/site-header";

export default function ModelsPage() {
  return <main><SiteHeader active="Models" /><section className="page"><div className="shell"><span className="eyebrow">Deployed models / artifact facts</span><h1>Small, separate, inspectable.</h1><p>The site deploys two selected artifacts. They return independent review signals, never a single verdict about who wrote a document.</p><div className="content-grid"><article className="content-card"><Cpu size={28} color="var(--signal)" /><h2>Raw pattern model</h2><p>An experimental ONNX model that compares a text pattern with its training data. It is useful for demonstration and should never be treated as proof.</p></article><article className="content-card"><Stack size={28} color="var(--signal)" /><h2>Humanizer V4.8</h2><p>A multi-window model trained to look for signals that can remain after AI text has been transformed or rewritten.</p></article><article className="content-card"><Gauge size={28} color="var(--signal)" /><h2>Review bands</h2><p>Results are shown as plain-language review bands rather than confidence percentages. A low result never clears a document.</p></article></div><div className="page-note">The models are a class-project demonstration of careful testing, trade-offs, and model limitations—not an authorship verdict system.</div></div></section></main>;
}
