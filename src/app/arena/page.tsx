import Link from "next/link";
import { ArrowsLeftRight, ArrowRight } from "@phosphor-icons/react/dist/ssr";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";

export default function ArenaPage() {
  return <main><SiteHeader active="Model Arena" /><section className="page"><div className="shell"><span className="eyebrow">Model arena / live comparison</span><h1>Two models. One document. No invented consensus.</h1><p>LockDown AI runs the raw-AI and humanized-AI lanes independently. Agreement and disagreement are calculated only after you submit real text to the Detector.</p><div className="content-grid"><article className="content-card"><ArrowsLeftRight size={28} color="var(--signal)" /><h2>Independent lanes</h2><p>The ONNX raw-AI model and V4.8 humanizer do not share a score or a hidden ensemble.</p></article><article className="content-card"><ArrowsLeftRight size={28} color="var(--signal)" /><h2>Different questions</h2><p>One lane looks for raw AI characteristics. The other looks for machine-origin signals that remain after certain transformations.</p></article><article className="content-card"><ArrowsLeftRight size={28} color="var(--signal)" /><h2>Real-state only</h2><p>There is no preset arena result. If one model cannot run, it will show unavailable instead of a synthetic comparison.</p></article></div><div className="page-note">Run a document to see the live comparison. A model output is a signal for further review, not evidence of authorship.</div><Link className="analyze" style={{ marginTop:24 }} href="/"><ArrowRight size={17} weight="bold" />Open Detector</Link></div></section><SiteFooter /></main>;
}
